#!/bin/zsh

cd "${0:A:h}"

PROFILE="--profile"

TEST_DIR="test/verilog/pos"
TEST_NAME="tr-submodule-01"
TEST_FILE="${TEST_DIR}/${TEST_NAME}.v"
TEST_ANNOT="${TEST_DIR}/annot-${TEST_NAME}.json"

./iodine --build ${PROFILE} && \
	./iodine ${PROFILE} "${TEST_FILE}" "${TEST_ANNOT}" --trace
